127.0.0.1 localhost localhost.ansible.esxi71.local

{% for host in groups.all if host != 'localhost' %}
{% if hostvars[host].ansible_host is defined %}
{{ hostvars[host].ansible_host }} {{ host }} {{ host }}.ansible.esxi71.local
{% endif %}
{% endfor %}
